{
    "type": "object",
    "properties": {
        "menu": {
            "type": "array",
            "items": {"$ref": "#/definitions/menu_item"}
        }
    },
    "required": ["menu"],
    "definitions": {
        "menu_item": {
            "type": "object",
            "properties": {
                "id": {"type": "string"},
                "title": {"type": "string"},
                "type": {"enum": [
                    "int8", "uint8", "int16", "uint16", "int32", "uint32",
                    "float", "string", "callback", "pointer"
                ]},
                "role": {"enum": [
                    "simple", "factor", "fixed", "callback", "pointer"
                ]},
                "default": {},
                "limits": {"type": "object"},
                "callback": {"type": "string"},
                "data_ptr": {"type": "string"},
                "items": {
                    "type": "array",
                    "items": {"$ref": "#/definitions/menu_item"}
                }
            },
            "required": ["id", "title"],
            "oneOf": [
                {
                    "required": ["type", "role"],
                    "not": {"required": ["items"]}
                },
                {
                    "required": ["items"],
                    "not": {"required": ["type", "role"]}
                }
            ]
        }
    }
}