# 🧪 Модульные и smoke-тесты (`tests/`)

> Этот документ описывает **модульные и smoke-тесты** в директории
> [`tests/`](../tests). Сквозные (интеграционные) тесты описаны в
> [test_ru.md](./test_ru.md).

---

## 1. Общее описание

В `tests/` находятся быстрые тесты без внешних зависимостей, проверяющие отдельные
компоненты генератора: загрузку конфигурации, валидацию, флаттенинг, i18n, правила
данных и агрегацию. Вместе с интеграционным набором они образуют полный прогон
pytest (сейчас **54 теста, все проходят**).

Набор делится на:

- **smoke-тесты** — проверка импортов, загрузка/валидация/флаттенинг реального
  конфига;
- **модульные тесты** — точечные проверки `MenuData`, `MenuValidator`,
  `MenuFlattener`, `MenuDataAggregator` и модуля i18n.

## 2. Общие фикстуры

[`conftest.py`](../conftest.py) в корне проекта загружается до любых тестов и:

- добавляет корень проекта в `sys.path`, чтобы `import generate_menu` работал из
  любой рабочей директории;
- убирает устаревший `MENU_PROCESSOR_LANG` до импорта модуля i18n (перевод
  привязывается в момент импорта), что делает тесты детерминированными
  (английский язык);
- предоставляет фикстуры для всех тестовых модулей:

| Фикстура | Область | Что даёт |
|----------|---------|----------|
| `project_root` | session | Абсолютный путь корня проекта |
| `package_dir` | session | Абсолютный путь пакета `generate_menu` |
| `config_path` | session | Абсолютный путь `config/config.yaml` |
| `menu_config` | session | `MenuConfig` из реальной конфигурации |
| `menu_data` | session | `MenuData` из реальной конфигурации |
| `menu_validator` | function | Свежий `MenuValidator` на каждый тест (хранит внутреннее состояние) |
| `menu_flattener` | function | Свежий `MenuFlattener` на каждый тест |

## 3. Тестовые модули

| Модуль | Что проверяет |
|--------|---------------|
| [`test_smoke.py`](../tests/test_smoke.py) | Пакет импортируется без ошибок; реальный конфиг загружается, валидируется и флаттенится в 18 узлов, первый — `root`. |
| [`test_validator.py`](../tests/test_validator.py) | Schema + кастомная валидация: дубликаты id, правила веток/листьев, значения по умолчанию вне диапазона, границы индексов values/factors, вложенные пути ошибок (`parent->child`), идемпотентность `validate()`. |
| [`test_flattener.py`](../tests/test_flattener.py) | Флаттенинг и связи: количество узлов, флаги корневой ветки, `get_node_by_id`, флаги листа/ветки, циклические vs limit sibling'ы, явный vs умолчательный `navigate`, пустое меню → только root. |
| [`test_menu_data.py`](../tests/test_menu_data.py) | Правила типов/ролей/контролов/навигации: enum'ы, `c_type()`, роли, `get_controls_for_type`, правила навигации и значения по умолчанию, `get_control_config`. |
| [`test_menu_data_aggregator.py`](../tests/test_menu_data_aggregator.py) | `MenuDataAggregator` (пункт плана P2/A1): построение из flat-узлов, мемоизация `cached_property`, делегирование `MenuCraft` единому агрегатору, идентичность результатов. |
| [`test_i18n.py`](../tests/test_i18n.py) | gettext/Babel: язык по умолчанию английский, `get_language()` из `MENU_PROCESSOR_LANG`, английские сообщения без перевода, русский каталог применяется в отдельном подпроцессе. |

## 4. Запуск модульного набора

Из корня проекта:

```bash
# только модульные и smoke-тесты
python -m pytest tests -q

# весь набор проекта (модульные + интеграционные)
python -m pytest -q
```

## 5. Примечания

- Модульные тесты используют **реальную** конфигурацию и меню проекта
  (`config/`, `menu/`), поэтому дополнительные фикстуры или sample-файлы не нужны.
- `MenuValidator` хранит список уже встреченных id между вызовами `validate()`,
  поэтому тесты всегда создают свежий экземпляр (фикстура `menu_validator`).
- `MenuFlattener` не хранит состояния и переиспользуем; свежий экземпляр на тест
  всё равно предоставляется для изоляции.
- Тест i18n в подпроцессе принудительно задаёт `PYTHONIOENCODING=utf-8`, чтобы
  русский вывод не искажался на Windows (кодировка консоли по умолчанию — cp1251).
