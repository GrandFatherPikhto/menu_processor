"""PyQt6 GUI for MenuCraft.

Thin wrapper over the ``generate_menu`` package: edits the menu tree,
drives the existing validate -> flatten -> generate pipeline, and
displays results. No backend module is modified by this package.
"""

__version__ = "0.1.0"
